image: python 3.11

stages:
  - build
  - docs

include:
  - project: computing/gitlab-ci-templates
    file: conda.yml

build-package:
  stage: build
  extends:
    - .conda:build
  script:
    - !reference [".conda:build", script]
